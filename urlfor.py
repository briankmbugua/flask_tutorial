"""The url_for() helper function generates URLs from the information stored in the application's URL map.EXAMPLE- url_for('index') would return/.Calling url_for('index',_external=True) would instead return an absolute URL, which in this example is http://localhost:5000/
Dynamic URLs can be generated with url_for() by passing the dynamic"""
